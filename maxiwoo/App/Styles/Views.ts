import { ImageStyle, StyleSheet, TextStyle, ViewStyle } from 'react-native';

export const base: ViewStyle | ImageStyle | TextStyle = {
  flex: 1,
  flexDirection: 'column',
  justifyContent: 'flex-end',
};
